package com.jamesm92.nomadportal

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder unit test so `./gradlew test` (and CI) has something real to
 * run from the first commit. Replace/expand once actual logic (e.g. a
 * Kotlin-side Micron AST walker, once micron2compose exists) lands here.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
